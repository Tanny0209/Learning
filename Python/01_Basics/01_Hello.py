# LESSON: The print() function
# Purpose: This is my very first program! It teaches me the most basic way to
# show output on the screen using the built-in print() function.

# print() is a built-in function (Python gives it to me automatically).
# Whatever I put between the parentheses is written to the console.
# Here I pass a "string" (text wrapped in quotes) as the argument.

# When Python runs this line, it:
#   1. Reads the text inside the quotes: Hello, World!
#   2. Sends it to the console/screen so I can see it.
print("Hello, World!")
